# 2026-06-18 工作日志

## DomeGallery 画廊动效迭代

1. **BorderGlow + hover 放大**（较早）
   - 集成 React Bits BorderGlow 组件，包裹每张图片
   - 添加 hover scale(1.08) 动效
   - BorderGlow 颜色: 橙色 #F26522 / 粉色 #f472b6 / 蓝色 #38bdf8

2. **移除 BorderGlow，恢复原始状态**
   - 用户要求"返回上一步"
   - 删除 BorderGlow.jsx / BorderGlow.css
   - DomeGallery.jsx 移除 BorderGlow import 和包裹代码
   - DomeGallery.css 恢复 overflow: hidden，移除 hover scale 和 BorderGlow 样式

3. **添加简洁 hover 放大动效**（当前最终状态）
   - DomeGallery.css `.item__image` 添加 `transition: transform 300ms ease`
   - 新增 `.item__image:hover { transform: translateZ(0) scale(1.06); z-index: 10; }`
   - 无 BorderGlow，仅微微放大效果
   - overflow: hidden 确保放大不溢出圆角

## 当前文件状态
- `src/DomeGallery.jsx`: 原始版本（无 BorderGlow）
- `src/DomeGallery.css`: 原始版本 + hover scale(1.06) 动效
- `src/App.tsx`: `fit={0.85}` 铺满左右
- BorderGlow.jsx / BorderGlow.css: 已删除
